{% extends '_full.html.twig' %}

{% set seo_title = meta_subtitle ~ ' anpassen' %}

{% block page_header %}
<section class="py-3">
    <div class="container">

        {{ include('component/settings_subbar.html.twig',{'selected':'account'}) }}

        <div class="row g-3">
            <div class="col-lg-12">
                <div class="card bg-light-subtle rounded-2 shadow-sm border-0">
                    <div class="card-body  border-bottom border-primary">
                        <h3 class="h6 mb-0 text-center text-lg-start">Daten aktualisieren</h3>
                    </div>
                    <div class="card-body bg-white">
                        {{ form(form) }}
                    </div>
                    <div class="card-body rounded-bottom-2 border-top"></div>
                </div>
            </div>
        </div>
    </div>
</section>


{% endblock %}

{% block body %}{% endblock %}
{% block aside %}{% endblock %}

